# Flamegrahphs of Verite

This directory contains the flamegraphs of the compaigns.

The `verite::executor::VeriteExecutor<...>::run_input` represents the EVM emulation overhead and takes the most computation resources.
